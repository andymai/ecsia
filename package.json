{
  "name": "ecsia-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc -b",
    "clean": "tsc -b --clean",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -b --noEmit false",
    "bench": "vitest bench --run"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "bitecs": "^0.3.40",
    "fast-check": "^3.23.0",
    "miniplex": "^2.0.0",
    "tinybench": "^3.0.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
